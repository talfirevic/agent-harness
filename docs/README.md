# Docs

Documentation under `docs/` is part of the system of record and must stay synchronized with requirements and planning artifacts.

Core verification commands:
- `./scripts/verify-env`
- `./scripts/harness/knowledge-contract.sh`
- `make verify`
