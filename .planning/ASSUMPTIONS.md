# ASSUMPTIONS

- Default stack: `typescript-node`.
- Default deployment target: `github-actions-docker`.
- Default compliance profile: `standard-web`.
- Non-blocking unknowns are defaulted and recorded here.
